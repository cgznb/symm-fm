extends: mu_glioma_zscore_cfm_5090.yaml
project:
  name: ispy2-symmflow3d-mu-glioma-zscore-source-bridge-cfm
  output_dir: outputs/mu_glioma_zscore_source_bridge_cfm_5090
baseline_training:
  cfm_base_distribution: source_gaussian
  cfm_noise_scale: 0.25
  endpoint_validation_pairs: 8
  endpoint_validation_samples: 2
  endpoint_validation_steps: 1
  endpoint_validation_solver: euler
  checkpoint_metric: endpoint_mean_mse
