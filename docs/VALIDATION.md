# 发布验证

日期：2026-09-16。独立 venv、CPU、受限线程；没有运行正式训练、模型下载或患者推理。

| 检查 | 结果 |
|---|---|
| 核心 flow、solver、sampler、conditions、velocity、checkpoint、training 与 CLI | 88 passed |
| 三相位、数据准备、first-post 合同与恢复 | 75 passed |
| ROI32、T0 裁剪与 BF16 评估回归 | 42 passed |
| 发布路径/私有配置测试 | 7 passed |
| 主要 CLI 帮助入口 | 10/10 passed |
| 本地 editable 安装 | passed（不重复安装第三方依赖） |

三相位首次测试暴露了结构读取误触发无关历史 VQ 字段的问题；修正为显式结构读取后全部通过。
实际 VQ/数据加载仍进行原兼容性检查。ROI32 评估的 BF16 到 NumPy 转换仅在发布副本修复，
新增回归证明与显式 FP32 转换一致。原训练目录没有修改，也没有恢复或重启原任务。

验证环境主要版本：Python 3.12、PyTorch 2.12.1、NumPy 2.4.6、MONAI 1.6.0。
核心包安装声明调整为覆盖已验证依赖范围；并未测试该范围内的每种组合。
保留了库弃用与 CPU 环境 pin_memory 警告。

真实 MRI、VQ 预训练权重、ITK 变换运行、nnU-Net、完整 GPU 训练与下游 pCR 评估没有重跑。
完整预处理仍需自行准备数据、配套权重及可选依赖，合成测试不等于这些环节已完成临床验证。

发布文件经过语法、文件类型、大小、符号链接、凭据模式及文档配置中的校验值检查。
上传后的新克隆回读与入口核对另存本地发布审计。
